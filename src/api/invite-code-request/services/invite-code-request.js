'use strict';

/**
 * invite-code-request service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::invite-code-request.invite-code-request');
