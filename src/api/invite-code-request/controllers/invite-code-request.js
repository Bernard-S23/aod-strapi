'use strict';

/**
 *  invite-code-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::invite-code-request.invite-code-request');
